{{app}}.directive('{{directive}}', [
  function () {
    return {
      restrict: '{{restrict}}',
      scope: {},{{template}}
      link: function ($scope, element, attrs) {        
        
      }
    };
  }]);